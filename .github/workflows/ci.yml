name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  # Debug info makes the Bevy test binaries large enough to exhaust a hosted
  # runner's disk while linking; tests do not need it.
  CARGO_PROFILE_DEV_DEBUG: 0
  # The nightly the dev shell's flake.lock pins. A floating nightly would let a
  # new clippy lint turn CI red with no change to the code.
  RUSTUP_TOOLCHAIN: nightly-2026-06-12

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Toolchain
        run: rustup toolchain install "$RUSTUP_TOOLCHAIN" --profile minimal --component clippy,rustfmt
      # `.cargo/config.toml` links through clang + mold.
      - uses: rui314/setup-mold@v1
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@nextest
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: cargo nextest run --workspace
